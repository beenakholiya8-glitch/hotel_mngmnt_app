_schema-version: 3.3.0
ID: hotel_mngmnt_app
description: A simple CAP project.
version: 1.0.0
modules:
- name: hotel_mngmnt_app-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: hotel_mngmnt_app-auth
  - name: hotel_mngmnt_app-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: hotel_mngmnt_app-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: hotel_mngmnt_app-db
  parameters:
    buildpack: nodejs_buildpack
- name: hotel_mngmnt_app-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: hotel_mngmnt_app-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comhmahotelmngmnt.zip
      name: comhmahotelmngmnt
      target-path: resources/
- name: comhmahotelmngmnt
  type: html5
  path: app/hotel_mngmnt
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: hotel_mngmnt_app-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: hotel_mngmnt_app-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: hotel_mngmnt_app-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: hotel_mngmnt_app-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: hotel_mngmnt_app-html5-service
    service-plan: app-host
- name: hotel_mngmnt_app-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: hotel_mngmnt_app-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
    - npx rimraf gen/db/src/gen/data
